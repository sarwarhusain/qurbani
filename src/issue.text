1 issue:when i render the product by category or id ,you know i have a massive problem with this project,i could not render the file,beCz or dynamic id.i was remark the newsAuth also find from google, but not rendering the using dynamic id.suddenly i remember one of the project i have facing the same problem,(if i use server it should be work , i have use public/data.json) ,then i found that and solve it,its a very simple task ,like: check the id ,if current id is true than move.i don't know its authentic or not,but its work.

2 issue is: sort by price, its a simple think ,where you want to order it means where the sort should be work,first set the place, if the place is rook file,first of all render the data by parent folder ,then fetch the data or move the data, and the current component should be client , now the revive the item and copy the item using spreed op,then getting the map by sorted value then it wold be work definitely,make sure when fetch the data from parent component it could be async
